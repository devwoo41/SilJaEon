#### 자연어처리의 주요 전처리 과정

- 토큰화

#### 토큰화 (Tokenization)

- 문자를 연속적인 단위인 토큰으로 분해하는 것, 영어에서는 일반적으로 공백 문자 또는 구두점으로 구분된 단어 및 문자 시퀀스를 토큰으로 간주, 한국어에서는 일반적으로 형태소 단위로 토큰화.


- 최근에는 *서브워드(subword)*라고 불리는 토큰 단위를 주로 사용한다.

#### 단어 정규화 (Word Normalization)

- *Case folding*: 모든 단어를 대문자 또는 소문자로 일치
ex. I am a boy. -> i am a boy. / APPLE -> apple

- *Lemmatization*: 단어의 뿌리를 의미하는 기본 사전형 단어(어근)으로 변환
ex. is, are, am -> be / dinner, dinners -> dinner

- *Stemming*: 어형 변화의 기초가 되는 부분이자 단어의 의미를 담고 있는 단어의 핵심 부분을 추출
ex. am -> am / Running -> Run / Studies -> Studi

#### 불용어 처리 (Stopword Removal)

- 분석에 큰 의미가 없는 단어를 제거
ex. 대표적인 불용어 -> [I, my, me, myself, we ...]

- 자연어처리 과제 종류에 따라 불용어는 달라질 수 있다.

#### 벡터화 (Vectorization)

- 단어, 문장, 문서를 컴퓨터가 이해할 수 있도록 숫자화해주는 과정
```
등장 횟수 기반의 단어 표현
- 희소표현이라고도 불리는데, 여기에는 방법이 세가지가 있다. One-hot encoding, BoW(Bag of Words), TF-IDF(Term Frequency-Inverse Document Frequency)
```
- One-hot encoding: 단어의 존재 여부를 벡터로 표현하는 방법

- BoW: 단어의 등장 횟수로 벡터를 표현하는 방법

- TF-IDF: 특정 문서에만 더 자주 등장하는 단어에 가중치를 두는 벡터화 방법
```
분포 기반의 단어 표현
- 밀집표현이라고도 불리고, 4가지 정도의 방법이 있다.
Word2Vec, GloVe, ELMO, BERT
```

#### 전통적인 자연어처리의 단계

- 형태소 분석 : 문장의 의미를 가진 가장 작은 단위인 형태소로 분리
ex. 나는 사과를 먹었다.
나/명사 + 는/조사 + 사과/명사 + 를/조사 + 먹/동사 + 었다/과거형어미

- 구문 분석 : 문장을 구성하는 성분들간의 관계를 분석
=> 의존 구문 분석: 문장 구성 성분간의 의존관계를 분석

- 의미 분석 : 문장이 나타내는 의미를 분석하여 표현
=> 단어 중의성 해소 : 단어가 현재 문장에서 어떤 의미인지 분석
=> 의미역 결정 : 서술어와 관계 있는 논항의 역할을 결정

- 화용 분석 : 문장을 해석해서 화자의 의도를 파악하는 단계
=> 상호 참조 해결
=> 화행 분석

#### 머신러닝이란?

- 머신러닝은 컴퓨터가 대용량 데이터에서 지식이나 패턴을 찾아 학습하고 예측을 수행하는 것을 의미한다.

- 학습 : 데이터를 사용하여 모델이 특정 목적을 달성하도록 하는 과정

- 예측 : 학습으로 생성된 모델에 새로운 데이터를 적용하여 결과를 예측, 머신러닝의 목표는 예측을 잘하는 것이다.

#### 머신러닝의 학습 과정

- 학습 데이터 준비 : 학습할 데이터의 가이드라인 구축, 수집, 검증, 가공한다. 머신러닝 모델은 데이터로부터 학습하고 진화한다. 

- 특성 추출 : 머신러닝에서 컴퓨터가 스스로 학습하고 입력받는 데이터를 분석하여 일정한 패턴이나 규칙을 찾아내려면 데이터를 컴퓨터가 인지 할 수 있게 변환이 필요하다. 데이터별로 어떤 특징을 가지고 있는지 찾아내고 그것을 토대로 데이터를 벡터로 변환하는 작업을 의미한다.

- 머신러닝 알고리즘 적용 & 모델 생성: 머신러닝의 학습 단계에서 얻은 최종 결과물을 가지고 실행한다.
```
모델의 학습 절차
1. 모델 선택
2. 모델 학습 및 평가
3. 평가를 바탕으로 모델 업데이트
=> 모델의 학습 절차 세 단계를 반복하면서 주어진 문제를 가장 잘
풀 수 있는 모델을 찾는다.
```

###### 머신러닝의 예측과정 정리
=> 테스트 데이터 준비 -> 특성 추출 -> 예측 및 결과 도출
